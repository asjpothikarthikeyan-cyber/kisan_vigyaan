const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith('.jsx') || file.endsWith('.js')) results.push(file); } }); return results; } const files = walk('src/components'); files.forEach(f => { let content = fs.readFileSync(f, 'utf8'); let newContent = content.replace(/ select-none/g, '').replace(/select-none /g, '').replace(/"select-none"/g, '""').replace(/select-none/g, ''); if (content !== newContent) { try { fs.writeFileSync(f, newContent); console.log('Fixed', f); } catch(e){} } });
